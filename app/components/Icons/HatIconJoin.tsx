import styled from "styled-components/native";
import { colors } from "../colors";

export const HatIconJoin = styled.Image`
resize-mode: stretch;
position: absolute;
`;


///передать сюда ссылку на source
